// Playground bundle for the settings page: the background and the options UI in one page, over the chrome shim.
// One-click OpenRouter sign-in needs chrome.identity and is not available here; the paste-a-key path and the
// connection test work.
import './boot';
import '../background/index';
import '../options/index';
